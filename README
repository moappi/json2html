json2html is a simple but powerful free jQuery plug-in used to render json to html. 
Using custom defined JSON transforms json2html will convert a JSON array of objects, such as an RSS feed, into an unstructured list. 

For more usage information & examples please visit 

www.json2html.com